<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bishram.jharkhand.tour">

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@drawable/jharkhand_tour_guide_launcher_icon"
        android:label="@string/app_name"
        android:theme="@style/TranslucentTheme"
        android:roundIcon="@drawable/jharkhand_tour_guide_launcher_icon"
        android:supportsRtl="true">
        <activity
            android:name="com.bishram.jharkhand.tour.activity.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/main_activity_label" />
        <activity
            android:name=".activity.DetailActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_detail"
            android:theme="@style/FullscreenTheme"/>
    </application>

</manifest>